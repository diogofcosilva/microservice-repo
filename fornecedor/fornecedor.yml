spring:
  datasource:
    username: postgres
    password: admin
    url: jdbc:postgresql://localhost:5432/fornecedor
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true